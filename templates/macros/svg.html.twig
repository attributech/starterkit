{% macro inline(name) %}
<div class="icon icon--{{ name }}">
  {% include active_theme_path() ~ '/dist/images/' ~ name ~ '.svg' %}
</div>
{% endmacro %}

{% macro use(name) %}
<svg class="icon icon--{{ name }}">
  <use xlink:href="{{ active_theme_path() }}/dist/images/sprite.svg#images--{{ name }}"></use>
</svg>
{% endmacro %}
